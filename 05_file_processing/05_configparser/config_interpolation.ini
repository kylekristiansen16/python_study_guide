[DEFAULT]
host = localhost

[mariadb]
name = hello
user = user
password = password

[redis]
port = 6379
db = 0
dsn = redis://%(host)s  # Placing any key between % and s informs the parser of the need to interpolate, host will be replaced with localhost
